        <% tag_list.each do |tag|%>
        <%= link_to tag_books_path(tag_id: tag.id) do %>
          <span><%=tag.name %></span>:
          <span><%="(#{tag.books.count})" %></span>
        <% end %>
        <% end %>